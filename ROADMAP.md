# Roadmap

Cada etapa é pensada para gerar valor sozinha — e virar conteúdo (post/vídeo) ao ser concluída.
A visão de produto completa (referências de mercado, backlog e decisões de design) está em [docs/VISAO.md](docs/VISAO.md). As fontes de dados, APIs e o porquê de cada uma estão em [docs/FONTES.md](docs/FONTES.md). **v1 = 100% FIIs.**

## Fase 1 — Fundação (MVP CLI, foco em FIIs)

- [x] **Coletor CVM** — informes mensais de FII (2016+) baixados para SQLite local (`~/.scout`), com ticker→CNPJ resolvido pelo ISIN e normalização dos dois vocabulários de coluna (pré e pós Resolução CVM 175)
- [x] **Indicadores** — cotação de mercado (Yahoo Finance, candles mensais desde 2011, sem chave), oscilações 12m e no ano, P/VP atual e média histórica, PL, VP/cota (média ajustada por desdobramento), nº de cotas, valor do ativo, DY mensal (fração→%, com filtro de lixo auto-declarado), cotistas
  - sincronização de cotação é preguiçosa (1x/dia por ticker) e degrada com aviso quando offline
  - pendente para depois: rendimento em R$ por cota, receita (informe trimestral)
- [x] **Motor de red flags** — 6 regras auditáveis, cada uma com severidade, evidência numérica e fonte; regras sem histórico suficiente aparecem como "não avaliadas" (nunca como aprovação silenciosa)
  - distribuição acima da variação patrimonial · diluição destrutiva por emissão · VP/cota em queda relevante · base de cotistas (mínimo legal de 100 para isenção de IR, Lei 14.754/2023) · P/VP fora da faixa histórica · rendimentos irregulares
  - pendente para leva 2: vacância fora da curva (precisa do informe trimestral da CVM)
- [x] **M5 — Relatório HTML com gráficos** — página única auto-contida (gráficos SVG gerados em Python puro): cotação × VP/cota, P/VP com média histórica, DY e PL com alternância **Ano/Mês**, e **rentabilidade acumulada com proventos × CDI × IPCA** (fontes: Yahoo ajustado + SGS/Banco Central) em janelas de 12 meses/5 anos/máximo; selo-síntese de 5 níveis (critérios públicos — ver VISAO.md) no HTML e no terminal; data de atualização por fonte; disclaimer; `scout analisar TICKER --html` salva e abre no navegador
  - ~~pendente: linha do IFIX~~ RESOLVIDO (19/07/2026): fonte achada nas estatísticas históricas oficiais da B3 (`indexStatisticsProxy/GetPortfolioDay`, 1 request leve por ano) — IFIX é a 4ª linha do gráfico de rentabilidade
- [x] **M6 — Informe trimestral CVM** — coletor dos informes trimestrais (2016+); tabela de imóveis com área, % da receita, vacância e inadimplência POR IMÓVEL (terminal + HTML); indicador e gráfico de vacância ponderada por área; 3 red flags novas: **rendimentos vs resultado financeiro (exata, substitui o proxy)**, **vacância alta** e **fundo novo (<24 meses)**; motor ganhou supressão de regra (proxy some quando a exata roda)
  - gotcha de escala CVM: no mesmo arquivo, vacância/inadimplência são FRAÇÃO (1.0=100%) e % receita é PERCENTUAL (0-100)
  - ~~pendências~~ RESOLVIDAS (19/07/2026): agrupamento por estado (UF estimada pelo endereço — vale a última sigla válida), receita por setor de inquilino (arquivo `renda_acabado_inquilino` do trimestral), e regra de distribuição refinada com o `Resultado_Financeiro_Liquido_Acumulado` (reserva que cobre o excesso não dispara alerta)
- [x] **M7 — Raio-x do administrador** — seção "Administrador" no terminal e no HTML: outros FIIs da mesma casa com ticker (derivado do ISIN), idade, segmento e **selo individual** (calculado sem cotação), com link cruzado entre relatórios; migração automática da base (recarrega informes mensais para preencher o administrador histórico)
- [x] **M8 — Ranking / pesquisa avançada** — `scout ranking --por dy|pvp|pl|cotistas|cotacao --top N --sem-alertas --segmento X`: varre a base inteira (~1.300 fundos ativos em segundos), por padrão só fundos com ticker (negociáveis); "sem alertas" = selo verde/amarelo; critério e filtros sempre explícitos no rodapé ("fato ordenado, não recomendação"); relatório do fundo ganhou a seção **Pares do segmento** (5 maiores por PL + média do segmento, com selo e links cruzados)
  - P/VP e cotação nos rankings dependem do cache de cotações (o site do M9 terá todas)
- [x] **M9 — Site estático** — `scout site` gera índice buscável + página de cada FII negociável + rankings do dia + página de apoio; workflow do GitHub Actions (`.github/workflows/site.yml`) roda 1x/dia útil e publica no GitHub Pages (requer habilitar Pages: Settings → Pages → Source: GitHub Actions)

## Fase 2 — Camada qualitativa

- [x] **Coletor FNET** — `coleta/fnet.py`: lista documentos do fundo na API pública do FNET (B3), baixa o último relatório gerencial (PDF direto) com cache idempotente em `<dados>/documentos/<cnpj>/`
- [x] **LLM local (Ollama)** — `scout ia TICKER`: extrai o texto do PDF (pypdf), monta o contexto determinístico (indicadores + red flags) e pede ao modelo local APENAS a extração de fatos com citação do trecho — nunca números novos, nunca recomendação. Requer Ollama instalado (`winget install Ollama.Ollama` + `ollama pull qwen2.5:14b`)
- [x] **Fatos relevantes por IA** — ler também os fatos relevantes do FNET (prioridade: é o que falta para a promessa "lemos os documentos oficiais" cobrir os documentos que assustam) - FEITO: scout ia le os ultimos 3 fatos relevantes (cache idempotente, bloco por comunicado com data/resumo/citacao, --sem-fatos pula, escaneados apontados para leitura manual)
- [x] **Oscilações com contexto** — FEITO: seção na página do fundo com os meses de variação >±10% da cota (ajustada por desdobramento — split não vira oscilação falsa), cruzados com eventos factuais do mesmo período: emissão de cotas (base de cotas dos informes CVM), mudança brusca de rendimento por cota (±30%) e fatos relevantes já lidos pela IA (com link para o original no FNET). Nota nega causalidade explicitamente
- [x] **Gestor** — FEITO (fonte melhor que FNET: registro de fundos dos dados abertos da CVM, `registro_fundo_classe.zip`, 1 download semanal cobre todos os FIIs): seção "Gestora" na página com os outros fundos geridos pela mesma casa, cada um com selo/motivo/link — quem decide a estratégia, separado de quem faz a burocracia (o admin do MXRF11 administra 275 fundos; a gestora gere 28 — o cruzamento que informa é o da gestora). Quando gestora = administrador, as seções se fundem
- [x] **Comunicados ao Mercado e Assembleias na leitura por IA** — FEITO: o lote lê, além do relatório gerencial e dos 3 fatos relevantes, até 2 Comunicados ao Mercado e 2 Assembleias (AGO/AGE) dos últimos 12 meses — tudo numa ÚNICA chamada ao modelo (custo quase igual; corte de 5000 chars/doc respeita o contexto de 16k). JSON ganhou o bloco `comunicados` (com rótulo por documento; o legado `fatos` segue legível); a seção da página virou "Fatos relevantes, comunicados e assembleias" com link rotulado por original, e as Oscilações cruzam com todos os documentos lidos. Nota: os Informes Estruturados NÃO entram — são os mesmos dados que já baixamos da CVM em massa
- [x] **Demonstrações Financeiras (parecer do auditor)** — FEITO, e SEM IA: classificação determinística (`parecer.py`, regex sobre as fórmulas normatizadas NBC TA 700/705/570) do parecer na DF anual — sem ressalvas / com ressalva / adversa / abstenção + detecção de "incerteza de continuidade operacional" (o alerta máximo). Seção "Parecer do auditor" logo após as red flags, com borda na cor da gravidade, trecho citado em estilo log e link para a DF no FNET. Incremental por id da DF (anual = ~zero custo); prefetch baixa a DF junto. Pendente futuro: integrar ao SELO como red flag quando a cobertura da base estiver completa
- [x] **Histórico entre relatórios (IA + flags)** — FEITO (mecanismo): cada leitura do lote grava um `retrato` (selo, alertas, cota) no JSON; quando um relatório novo substitui o antigo, o retrato anterior é preservado em `anterior` e a página exibe o fato comparado ("na leitura de X o selo era Y com N alertas; desde então a cota variou Z%") com a redação anti-"previsão" explícita. O primeiro comparativo aparece naturalmente quando os fundos publicarem o próximo relatório
- [x] **Leitura de IA no relatório HTML** — FEITO junto com as leituras versionadas: secao "Leitura por IA" na pagina do fundo (leituras/*.json via scout ia-lote), com modelo, data, aviso de idade >40 dias e glossario
- [x] **Cotações oficiais B3 (COTAHIST)** — FEITO (pré-requisito de IP da VISAO antes da divulgação ampla): Yahoo removido; `coleta/b3.py` baixa a Série Histórica oficial (anuais 2011+ uma vez + mensal do ano corrente, refresh 1x/dia), agrega por mês e reconstrói as séries com ajustes PRÓPRIOS e auditáveis — desdobramento (mesmo algoritmo do VP) e retorno total por proventos (DY CVM × VP ajustado, ancorado no preço atual). Um arquivo cobre a base inteira: morreram as 568 requisições+sleep do build do site. Preço exibido = fechamento oficial D-1 (tempo real é serviço licenciado da B3); textos/tooltips/fontes atualizados
  - CORREÇÃO (20/07/2026): a B3 só publica o arquivo MENSAL depois do mês fechar — o desenho original deixava o preço parado no fim do mês anterior (e o 404 do mensal corrente DERRUBAVA o `atualizar` no meio, impedindo as cargas seguintes). O mês corrente agora vem dos arquivos DIÁRIOS (`COTAHIST_D`, ~0,5 MB, merge incremental idempotente; feriado 404 é marcado e não volta); quando o mensal sai, substitui o acumulado (autocorreção na virada)

- [x] **Fundos encerrando: situação cadastral e deslistagem** — FEITO (20/07/2026, decisão do dono): red flag ALTA "Fundo em liquidação"/"Registro cancelado" (situação do registro CVM) na página, ranking e relacionados de FIIs **e** ETFs — fundo em liquidação continua enviando informes (o corte de atividade não o pega; a flag pega: 38 casos reais na base, ex. HGPO11) e NÃO sai do site (quem tem cota precisa ver o aviso), mas sai da fila do `ia-lote` (não gastar leitura com quem está encerrando); ETF que some da listagem da B3 vira `listado=0` no sync semanal → sai do site e do lote (deslistado não negocia; linha de curadoria manual sem id_fnet nunca é mexida; listagem vazia = falha da fonte, não deslista ninguém)

## Fase 3 — ETFs (próxima vertical; entendimento registrado em [docs/ETFS.md](docs/ETFS.md))

- [x] **E1 — Fundação de dados** — FEITO (19/07/2026): `coleta/b3fundos.py` usa a API `fundsListedProxy` da B3 (a mesma do site oficial) com os três segmentos descobertos — **ETF (120) · ETF-RF (57) · ETF-Cripto (26) = 203 fundos** com ticker+CNPJ na tabela `etfs` (refresh semanal no `atualizar`); COTAHIST ganhou o codbdi 14 (preços dos ETFs de renda variável/cripto no mesmo pipeline dos FIIs); planilha de curadoria com 201 tickers preenchidos e RF/Cripto auto-confirmados pelo segmento B3. Descobertas que mudaram o plano: informe diário CVM só tem 6 dos ETFs (cota patrimonial NÃO vem de lá — rota identificada: relatórios estruturados da B3, `GetStructuredReports`, fica no E2 junto com o CDA); XFIX11 fora da listagem B3 (curadoria manual)
- [x] **E1.5 — Verificador de classificação (carteira real vs curadoria)** — FEITO (19/07/2026): o CDA da CVM tem arquivo próprio para fundos de índice (`cda_fie`, 182 ETFs cobertos) com posições E o PL mensal; `coleta/cda.py` roda no `atualizar` (mensal), grava composição por grupo (Renda Fixa/Ações/Exterior/Cotas de Fundos) + PL nas tabelas `etf_carteira`/`etf_pl`, e compara com `dados/classificacao_etfs.csv` — divergência dura vs ponto de atenção (fundo novo ~100% RF em captação; exposição via cotas de outro fundo) em `~/.scout/etf_divergencias.csv`. Planilha reestruturada: 1ª classificação = segmento B3, 2ª = classificação Scout (curadoria 212/212 completa)
- [x] **E2 — Raio-x de ETF (página própria)** — FEITO (19/07/2026): `relatorio/etf_html.py` com a **carteirinha de regras das 8 classes** (distribuição, tributação com fonte legal, pegadinhas), cards (preço oficial D-1, variação 12m ajustada, PL do CDA, classe Scout + segmento B3, gestora), composição da carteira, gráfico de cotação e rentabilidade vs CDI/IPCA/IFIX (preço ajustado = retorno total nos acumuladores); site ganha `etfs.html` (filtro por classe + busca) e link no índice; `scout analisar TICKER` reconhece ETF e abre a página própria; cadastro CVM passou a incluir FIIM (gestora dos ETFs); validado com dados reais (140 ETFs com preço; BOVA11 R$ 169,05 · PL R$ 13,4B · carteira 93% ações). Pendências movidas: cota patrimonial diária/prêmio-desconto (rota: relatórios estruturados B3) e cotistas — E4/E6
- [x] **E3 — Red flags de ETF** — FEITO (19/07/2026): `etf_flags.py` com 5 regras próprias — PL inviável (<R$ 30M, ALTA: ETF não paga os custos, encerramento é questão de tempo) / PL pequeno (<R$ 100M, MÉDIA) / liquidez baixa (<R$ 100k por pregão, MÉDIA — volume financeiro agora capturado do COTAHIST, VOLTOT) / histórico curto (<12 meses, BAIXA) / carteira não divulgada (BAIXA) / carteira em desacordo com a classe (BAIXA, reutiliza o verificador do CDA). Selo de 5 níveis reutilizado; "sem dado = não avaliada", nunca aprovação silenciosa. Selo na página e na listagem etfs.html. Pendentes de dados futuros: prêmio/desconto (precisa de cota patrimonial — E4) e tracking difference/taxa vs pares (precisa do índice de cada fundo)
- [x] **E4 — Preço de ETF de renda fixa** — FEITO (19/07/2026): fonte oficial encontrada — a API de cotações da própria B3 (`cotacao.b3.com.br/mds`, a que alimenta o site deles; o payload confirma `mkt: RendaFixaPrivada`). `coleta/b3rf.py` coleta o fechamento diário dos 57 (1x/dia no `atualizar` e no build do site), com upsert idempotente por pregão (fim de semana não duplica; volume e nº de pregões acumulam no mês) — validado real: 55/57 coletados (IMAB11 R$ 113,82). SEM histórico retroativo público: a série nasce da coleta diária e engorda com o tempo (honestidade > mágica); enquanto isso a página já mostra preço oficial, PL, carteira e regras
- [x] **E5 — Home multi-classe com mega-menu** — FEITO (19/07/2026): `index.html` virou a HOME (headline + busca ao vivo + bloco por classe com contagens e pills de classe de ETF); a tabela de FIIs mudou para `fiis.html` (com âncora #rankings); menu superior com dropdowns FIIs/ETFs (HTML/CSS/JS puro) compartilhado por home/fiis/etfs/comparar, com atalhos por classe (`etfs.html?classe=X` pré-seleciona o filtro); **busca com resultados ao vivo** na home: dropdown com ticker, nome, ponto colorido do selo e badge da classe (índice compacto embutido — FIIs + ETFs), navegável por teclado (↑↓ Enter Esc); comparador rotulado "Comparar FIIs" (guarda de classes); aviso de beta mora na home. Validação real: 190 páginas de ETF publicadas (renda fixa entra via carteira/PL mesmo sem preço de pregão)
- [x] **E6 — ETFs distribuidores de renda** — FEITO (20/07/2026): descoberta-chave — os ETFs estão INTEGRALMENTE no FNET (a investigação via API da B3 revelou `urlFundosNet` nos documentos), e cada provento é um XML estruturado ("Aviso aos Cotistas - Estruturado / Proventos em dinheiro") com valor por cota, datas e o aviso `RendimentoIsentoIR: Não` (diferente de FII!). `coleta/etf_renda.py` varre semanalmente e grava em `etf_proventos`; a página ganha o card "Distribui renda" (R$ X/cota · pago em DATA · NÃO isento de IR) e a carteirinha ganha a linha da geração DISTRIBUIDORA dinamicamente
- [x] **E7 — IA nos documentos de ETF** — FEITO (20/07/2026) e mais simples que o planejado: como os ETFs vivem no FNET, o `ia-lote` simplesmente passou a incluí-los no fim da fila — o fluxo `sem_relatorio` existente lê fatos relevantes/comunicados/ASSEMBLEIAS (onde vivem troca de índice, taxa e encerramento) e o parecer da DF; a página de ETF renderiza a leitura por IA e o parecer com as mesmas seções do FII
- [x] **E8 — Reclassificação automática de ETF** — FEITO (19/07/2026, decisão do dono): nas divergências DURAS (pontos de atenção não disparam), o Scout corrige a classe sozinho em camadas — determinístico inequívoco (grupo 1:1 ou segmento B3 manda) → palavra-chave nos nomes das posições (BITCOIN→Cripto, S&P→Ações Intl, GOLD→Commodities, TREASURY→RF Intl) → IA local (Ollama) lê os nomes no ambíguo que sobra, só quando disponível (Actions sem GPU pula). Salvaguardas: decide UMA vez (nunca re-rola), rastro em `reclassificacoes.csv` (data/de/para/origem/motivo), selo na página, humano sempre vence (`origem=manual`); conservador de propósito (o Safra Ibovespa 59%ações+41%RF estrutural não é tocado)
  - E6 refinado (19/07/2026): a varredura do FNET (que oscila ~17%, intermitente) virou **paralela (8 threads, só a rede; DB em série)** e **diária** — 2,7 min/dia em vez de 15 min serial, incremental e com progresso visível; `fnet.listar`/`baixar` com timeout/tentativas curtos no lote (ia-lote mantém os robustos)
- [x] **E9 — Taxa de administração de ETF (curadoria com fonte) — 176/203 (87%)** — FEITO (20/07/2026): confirmado por teste que **NÃO existe fonte oficial estruturada** de taxa de ETF (`registro_fundo_classe`, `cad_fi`, `extrato_fi`, `lâmina`, `perfil_mensal` da CVM, B3 `GetDetailFund`, brapi/Yahoo — nenhum traz; ETF migrou pro regime 175). Única fonte = **regulamento** do fundo. Curadoria com fonte em `dados/taxas_etfs.csv` (ticker; taxa % a.a.; fonte; data; **confianca**; **observacao** com página/trecho) + loader com **porteiro** (só entra quem tem taxa E confiança preenchida) + card na página + coluna na listagem + verbete no glossário. O passo `taxas_etf.atualizar` entra na cadeia do `scout atualizar`, **incremental por 2 parâmetros**: re-confere só por **frescor** (90 dias) e atualiza só se o **id do regulamento** mudar (`manual` protegido enquanto o documento não muda; pula no CI).
  - **A descoberta que destravou:** no regime 175 a taxa NÃO se chama "taxa de administração" — virou **"Taxa Global"** (adm+gestão+custódia fundidas), e mora numa **TABELA** (Quadro/Anexo) do regulamento ou da **"Alteração do Regulamento"**. Não era OCR — era o **pypdf embaralhando tabela**. Trocamos a extração por **PyMuPDF (fitz)**, que lê a tabela: cobertura saltou de **~11% (2/18) para 87% (176/203)**, quase tudo confiança alta, valores reais (BOVA11 0,10%, cripto mais caro). Nova dependência: `pymupdf` (import lazy).
  - **Extra por emissor:** o It Now (Itaú) tem API JSON limpa (`history-api-json` → `taxa_total_produto` + decomposição adm/gestão/custódia), usada como **fallback pontual** (fonte = página do emissor). Cada casa tem seu site — usado só como curadoria, não como raspador permanente.
  - **Restam ~27** ETFs de ~8 emissores bespoke cujo regulamento não baixou (FNET esgotado) → curadoria manual de baixa prioridade (`nao_achou` no arquivo + docs copiados à parte; preencher com `confianca=manual` + link do emissor)
- [x] **E10 — Carteira completa + reprecificação a "preço de hoje"** — FEITO (20/07/2026, desenho do dono): passamos a guardar a **carteira INTEIRA** de cada ETF (todas as posições, não só o top 10), com **quantidade** (`QT_POS_FINAL`) e **grupo** (tipo do ativo) por posição — migração v3 reprocessa o CDA. Página: top 10 em destaque + `<details>` "Ver carteira completa (N ativos)" com a lista inteira, e **nota DATADA** avisando que a posição é da competência X do CDA (a carteira de hoje pode diferir). **Resolvedor único de preço** (`precos.py`): cada posição pergunta o preço atual à fonte do seu tipo — e a descoberta-chave é que `cotacoes_meta` **já tem preço diário (D-1) de ação, FII e ETF** (COTAHIST 02/12/14, 1753 tickers), então **3 dos 4 tipos já acendem HOJE**; a página mostra a coluna "preço hoje" por posição + a **cobertura** (% do peso da carteira já reprecificável). Honestidade: o valor reprecificado é a quantidade da última competência × preço de hoje = **estimativa look-through**, não o NAV oficial (esse é a cota do ETF, que já mostramos)
  - [x] **Preço por ativo de TÍTULO PÚBLICO (RF)** — FEITO (22/07/2026): a investigação confirmou a rota — o CDA identifica título público por **CD_SELIC + DT_VENC** (CD_ATIVO/DS_ATIVO vêm VAZIOS — essas linhas eram descartadas e o IMAB11 ficava sem posições!), e o **mercado secundário da ANBIMA** (`ms{AAMMDD}.txt`, público, diário, sem auth) publica o PU indicativo com a MESMA chave, cobrindo TODOS os vencimentos (o Tesouro Direto só tem os de varejo — descartado). Implementado: parser CDA captura títulos (codigo=TPF+selic, coluna `vencimento` — migração v4 reprocessa o CDA sozinha na próxima carga), ramo ANBIMA no `precos.py` (1 download/rodada com recuo de até 5 dias p/ feriado; fora do ar = posição fica no valor do CDA, nunca inventa) e o NOME real do título (NTN-B, LTN…) vem da própria ANBIMA para a exibição
  - [ ] **Preço por ativo de debêntures / exterior / cripto** — restante do gap: debêntures (ANBIMA cobra auth no endpoint específico — reinvestigar), exterior e cripto seguem no valor do CDA. Bônus já possível: ETF **de FIIs** reprecifica 100% pela nossa base de FII diária — falta só um ETF de FII negociável aparecer na base

## Fase 4 — Ações (em andamento; dossiê em [docs/ACOES.md](docs/ACOES.md))

Tese: FRE lido por IA (diretoria/processos/partes relacionadas), parecer do auditor reusado, red flags societárias benchmarkadas contra casos reais (Americanas/IRB/Oi). Decisões aprovadas (20/07/2026): página por EMPRESA com N papéis · v1 = IBrX-100 · métricas universais primeiro (setoriais depois) · selo só após o benchmark.

- [x] **A1 — Fundação** — FEITO (20/07/2026): probes de TODAS as fontes registrados em ACOES.md (bônus: parecer do auditor vem em CSV na DFP; IPE 2026 ainda não existe); `coleta/empresas.py` sincroniza semanalmente IBrX-100 → 95 emissores/129 papéis (codeCVM = chave B3↔CVM; cadastro CVM 100% casado com setor/situação/AUDITOR); COTAHIST ganhou codbdi 02 (migração v4 rebaixa os arquivos 1x); ajuste de ação por eventos REAIS da B3 (desdobramento/bonificação = % novas ações, grupamento = razão direta; pares no mesmo dia — VIVT 40:1×80 — compõem por multiplicação) + retorno total com 8.088 dividendos/JCP reais de 120 tickers; validado: VIVT3 atravessa o split sem degrau, queda de ação nunca vira split falso (teste garante)
- [x] **A2 — Indicadores fundamentais** — FEITO (`coleta/fundamentos.py` + tabela `fundamentos`): DFP padronizada (contas CD_CONTA, sem parsear PDF) → receita, resultado bruto, EBIT, lucro líquido, ativo, PL, caixa, dívida bruta em **série anual (4 anos)**; extração robusta aos DOIS modelos de DRE (comercial × instituição financeira — lucro = maior 3.xx antes do 3.99; PL casado pelo NOME, não pelo código), escala MIL/UNIDADE aplicada. Indicadores derivados: **margem bruta/líquida, ROE, dívida líquida, dívida líquida/PL**; banco/seguradora sinalizado (`setor_financeiro`) com as métricas que não se aplicam nulas. Validado real: 93/95 empresas com balanço (Petrobras lucro R$ 37bi/2024, Ambev caixa líquido -R$ 16,9bi). Incremental por ano (só o mais recente rebaixa).
  - **Fatia 2 (20/07/2026): P/L, P/VP, DY e EBITDA.** `GetListedSupplementCompany` (já buscado p/ eventos) passou a capturar ações em circulação → `empresas.acoes_on/pn/total`. `fundamentos.multiplos()` (por ticker): P/L = preço/LPA, P/VP = preço/VPA (LPA/VPA = lucro/PL sobre ações_total), DY = 100·div12m/preço; None quando falta dado, P/L None em prejuízo, DY independe de ações. `multiplos_do_papel()` junta preço (cotacoes_meta) + ações + último balanço + `proventos_12m()`. **EBITDA = EBIT + D&A**: D&A vem da DFC (`DFC_MI_con`, ajustes operacionais `6.01.01.xx` com "deprecia/amortiza/exaust" — as amortizações de financiamento 6.02/6.03 ficam de fora); coluna `fundamentos.da`; `indicadores()` ganhou `ebitda`/`margem_ebitda` (None em banco). Validado no probe real: PETR D&A R$ 67bi, VALE R$ 16,5bi. **Falta a EXIBIÇÃO** (é o A4)
- [x] **A3 — Red flags societárias (regras + benchmark) — AGUARDANDO decisão do dono para ligar selo/exibição** — FEITO (22/07/2026): 6 regras em `acao_flags.py`, todas do MESMO zip DFP que já baixamos + FCA: (1) **parecer do auditor** — a própria CVM publica a classificação estruturada (`TP_RELAT_AUD`: Sem/Com Ressalva/Adverso/Negativa) no `dfp_cia_aberta_parecer` — sem PDF, sem regex de tipo; texto alimenta a detecção de continuidade; (2) **balanço reapresentado** (VERSAO>1); (3) **atraso na entrega** (DT_RECEB vs 31/03; >60d = ALTA); (4) **diluição por emissão** (composicao_capital, >20% de ações novas, com LPA antes/depois); (5) **proventos em ano de prejuízo** (B3 × DFP); (6) **troca frequente de auditor** (FCA com janelas de atuação — 1 download cobre o histórico). Coleta: `extrair_meta_ano` + tabela `dfp_meta` (mesma passada da DFP) + `atualizar_auditores` (FCA, marcador anual). **BENCHMARK RETROATIVO (obrigatório) PASSOU**: Oi DFP-2016 → Negativa de Opinião (ALTA) · IRB DFP-2019 → 324 dias de atraso (ALTA) + versão 5 reapresentada (MÉDIA) · Americanas DFP-2022 → Negativa de Opinião (ALTA) + 230 dias de atraso (ALTA) · controles WEG/Itaú/Petrobras DFP-2024 → ZERO flags (sem falso-positivo). Limitação anotada: zips ≤2019 usam outro vocabulário na composicao_capital (não afeta o produto, que cobre os últimos 4 anos). **EXIBIÇÃO LIGADA (22/07/2026, aprovada pelo dono após o benchmark)**: selo de 5 níveis + seção Red flags na página da ação, selo na listagem acoes.html e na busca viva. Salvaguardas anti-falso-positivo adicionadas na validação com a base real: razão implausível de ações (mil↔unidade no informe, caso RAPT) e evento societário B3 entre exercícios (desdobramento VIVT, bonificação GOAU) viram "não avaliada", nunca alerta; texto da flag de base de ações é factual (emissão/fusão/reestruturação). Base real: 62 Sem alertas · 29 Atenção · 1 Alerta grave — **Braskem (BRKM3) pega ao vivo com incerteza de continuidade operacional na DFP 2025**
- [x] **A4 — Página da empresa** — FEITO (22/07/2026): `relatorio/acao_html.py` no design novo — 1 página por PAPEL mostrando a EMPRESA (P/L, P/VP e DY são do papel consultado; papéis irmãos com link cruzado). Carteirinha de regras da classe (isenção R$ 20 mil/mês com fonte legal, JCP com retenção, ON vs PN vs unit, D-1), cards (cotação oficial, variação 12m ajustada, P/L com trato de prejuízo, P/VP, DY 12m com R$/ação, ROE, margem líquida, EBITDA+margem, dívida líquida/caixa líquido ×PL, lucro, liquidez/dia), tabela de papéis com múltiplos por papel, balanço anual DFP (série 4 anos + gráfico Receita×Lucro), cotação mensal e rentabilidade total vs CDI/IPCA; banco/seguradora omite as métricas que não se aplicam com aviso. `scout analisar TICKER` reconhece ação (fallback após FII/ETF)
- [x] **A5 — FRE estruturado + IPE (fatos relevantes)** — FEITO (22/07/2026) em 3 fatias: (1) `coleta/ipe.py` — o IPE 2026 PASSOU A EXISTIR (1519 fatos; o probe de 20/07 não o achava); índice anual cacheado em memória (1 download serve a fila toda, ≠ FNET), docs em formato FNET-compatível (id = hash 64-bit do protocolo — o protocolo em dígitos estouraria o INTEGER do SQLite), cache de PDF idempotente com validação de truncamento; (2) **95 empresas no fim da fila do `ia-lote`** via fluxo `sem_relatorio` (fatos relevantes + comunicados lidos pela IA local; a leitura renderiza em TODAS as páginas de papéis da empresa); (3) **FRE estruturado SEM IA** (`coleta/fre.py`, zip ~7 MB, marcador anual): seção "Quem manda na empresa" (conselho/diretoria/fiscal com cargo, na casa desde, % presença nas reuniões, badge "eleito p/ controlador", experiência declarada no tooltip) e "Transações com partes relacionadas" (top 8 por montante com relação/objeto/juros) na página da empresa; gotcha: FRE usa PONTO decimal (conversão pt-BR ingênua 100×-ava os montantes). Pendências futuras: ~~processos judiciais~~ FEITO em 23/07/2026 — o FRE do RAD é um zip com XML; os processos (4.3+) vêm como PDF EMBUTIDO em base64 + `ValorProvisionadoProcessosNaoSigilosos` estruturado. `fre_processos.py` extrai (validado Braskem: 654KB, texto legível), `fre_docs` guarda id/link do FRE vigente, o ia-lote lê por IA (prompt próprio: natureza/valor/chance de perda declarada, citando trecho; incremental por id — FRE é anual) e a página ganha a seção '⚖️ Processos judiciais (FRE)' com valor provisionado + link ao RAD. Resta a rodada real do lote (GPU do dono)
- [x] **A7 — Calculadoras de ação + histórico de proventos** — FEITO (22/07/2026, pedido do dono com referência de mercado): **Graham** (√(mult × LPA × VPA), prefill com LPA/VPA reais do último anual, multiplicador 22,5 editável; some honestamente em prejuízo/PL negativo — raiz de negativo não existe) e **Bazin** (preço-teto = dividendo anual ÷ DY mínimo 6% editável; base por botão: média dos últimos 5 anos CHEIOS × últimos 12 meses) — ambas no MESMO padrão opt-in da Gordon dos FIIs (aviso "não é recomendação" antes do botão, corpo escondido, premissas do usuário, sem veredito). **Histórico de proventos**: gráfico de barras R$/ação por ano (até 10 anos, base B3) com o DY do ano no topo de cada barra (proventos ÷ fechamento do fim do ano)
  - **Adiados com motivo** (referência Investidor10 avaliada em 22/07/2026): comparação por Setor/Subsetor/Segmento nos indicadores (com 95 empresas a média de setor tem 2–3 empresas e MENTE — entra quando a cobertura expandir); ~~checklist "Buy & Hold"~~ e ~~CAGR~~ FEITOS no A8
- [x] **A8 — ITR trimestral: lucro TTM + checklist de fatos + CAGR** — FEITO (22/07/2026): coletor ITR (`fundamentos_tri`, trimestres ISOLADOS — o DRE do ITR traz também o acumulado do ano, que é filtrado pela duração de ~3 meses; homólogo PENÚLTIMO preenche histórico; 6 anos, incremental por `ITR_<ano>`, 1.741 trimestres na base real). **P/L com lucro TTM** (anual + tris correntes − homólogos; só quando os pares fecham, senão anual — a base usada é rotulada no card; PETR4 validado: TTM 108,0bi = 110,6 + 32,8 − 35,3, P/L 4,97). **Checklist de FATOS** na página (8 critérios buy & hold verificáveis um a um com o número junto: 5 anos de bolsa, lucro nos últimos 20 trimestres — **T4 derivado**: anual − (T1+T2+T3), pois T4 isolado não existe no ITR —, ROE>10%, dívida<PL, dividendos 5/5 anos, CAGR receita/lucro no período, liquidez >R$2M/dia; ✓/✗/— sem nota e sem score: "✗ = o fato não se verifica, não 'ruim'"; banco/seguradora ganha "não se aplica"). DFP estendida de 4→6 anos (CAGR de 5 anos); validado real: ITUB 20/20 ✓, PETR pega os prejuízos de 2024-T2/T4, BRKM os de 2022–23
- [x] **A6 — Site (COMPLETO)** — FEITO (22/07/2026): `acoes.html` (listagem com busca + filtro por setor + rankings: maior DY / menor P/L com lucro / maior ROE), card Ações na home com contagem, dropdown Ações no menu superior, ações na busca viva com selo. **Comparador de ações** (`comparar-acoes.html`, fechado em 22/07): 2–3 papéis lado a lado com os mesmos fatos (selo+alertas, cotação, P/L, P/VP, DY, ROE, margem, EBITDA, dívida/PL, lucro/receita do anual), `?f1=&f2=` na URL, sem "vencedor" — e um aviso factual quando os papéis são de SETORES diferentes (P/L de banco não se compara com P/L de varejo)

- [x] **A9 — Expansão de cobertura: IBrX-100 → TODAS as listadas em bolsa** — FEITO (22/07/2026, decisão do dono): a busca paginada da B3 (`GetInitialCompanies`, ~3.500 registros) filtrada por **não-BDR + (segmento especial de listagem OU pregão recente no nosso COTAHIST)** — o teste definitivo de "negocia em bolsa", que pega o segmento tradicional sem ressuscitar deslistada antiga — dá **~376 companhias**. `no_ibrx100` virou flag INFORMATIVA (as 100 mais líquidas); o escopo de todos os coletores (DFP/ITR/FRE/FCA/eventos) e do site passou a ser a base toda. Empresa NOVA no sync dispara o backfill dos zips sozinha (deleta os marcadores DFP_/ITR_/FRE_/FCA_). Avisos de cobertura da home/listagem atualizados com a honestidade sobre small caps ("—"/não avaliada, nunca número inventado). Destrava a comparação setorial (próximo)
- [x] **A10 — Comparação setorial nos indicadores** — FEITO (22/07/2026, na sequência da expansão): cards de P/L, P/VP, DY, ROE e margem líquida ganham "**mediana do setor: X (N cias)**" — MEDIANA (média mente com outlier de small cap), só com **≥5 empresas com o dado**, computada 1× por build sobre a base toda (1 papel por empresa, para o emissor não contar 2×). Régua factual, não julgamento (acima/abaixo da mediana ≠ caro/barato). CLI de papel único sai sem a régua (não paga o custo da base toda — nunca inventa). Validado real: Consumo Cíclico P/L 5,5 (52 cias), Financeiro 8,8 (31), Utilidade Pública 15,2 (19)

## Instrumentação e operação
- [x] **Analytics sem cookie (GoatCounter)** — FEITO (19/07/2026): páginas vistas + termos de busca (anônimos, agregados), sem cookie e sem banner (LGPD-friendly); só liga com código configurado (`SCOUT_ANALYTICS`); a busca SEM resultado vira caminho próprio (demanda não coberta = combustível do roadmap); sanitizado a `[A-Z0-9]` (texto livre descartado); nota de transparência na página de apoio
- [x] **docs/FONTES.md** — referência única de todas as fontes/APIs e o porquê de cada uma

## Fase 5 — Demais classes
- [ ] **Renda fixa/CDB** — dossiê PRONTO em [docs/RENDAFIXA.md](docs/RENDAFIXA.md) (23/07/2026): tese = página por CONGLOMERADO emissor com saúde via IF.data (Basileia/lucro/captação), red flags com benchmark retroativo em liquidações reais (gate do dono p/ selo, como no A3), carteirinha do FGC (R$ 250 mil por CPF POR CONGLOMERADO + teto global R$ 1 mi/4 anos) e calculadoras opt-in (gross-up CDB×LCI/LCA, cobertura FGC). Achados do probe: olinda IFDATA com HTTP 500 nos endpoints de dados (lista OK) — R1 começa destravando a fonte (re-probe ou CSVs do portal); taxa de captação por banco NÃO tem fonte pública (comparação de taxas fora do escopo). Decisões aprovadas (23/07/2026). **R1 FEITO** (23/07/2026): o olinda destravou (o 500 era transiente — retry com espera no coletor); `coleta/bancos.py` sincroniza cadastro (TipoInstituicao=1 = conglomerado PRUDENCIAL, filtro Tcb b1/b2 → 660 emissores) e a série de 12 trimestres (relatórios 1-Resumo e 5-Capital, formato longo pivotado por prefixo do NomeColuna) em `bancos`/`bancos_tri` — ativo, carteira, captações, PL, lucro e **Basileia = 100·PR/RWA calculada** (validado real: Itaú 14,8%, BB 14,2%, Caixa 15,1%, Bradesco 14,9%, Santander 15,2%, BTG 15,9% em mar/2026); passo 'bancos (IF.data)' na cadeia do atualizar, incremental por trimestre. Pendências do R1: lista de associados do FGC (curadoria manual versionada) e refinar os 660 (b1/b2 inclui independentes minúsculas — corte por captações>0 no R2). Próximo: R2 (red flags + benchmark retroativo em liquidações reais, gate do dono p/ selo)
- [x] **Comparação entre ativos** — FEITO para FIIs (19/07/2026): `comparar.html` no site com 2-3 fundos lado a lado (mesmos fatos: selo com motivos, cotação, DY, P/VP, PL, cotistas, idade, alertas), links cruzados e `?f1=X&f2=Y` na URL; sem destaque de "vencedor" — comparação de fatos, não recomendação. Estende às outras classes quando existirem
- [ ] **API/site dinâmico** — expor o núcleo via FastAPI quando o site estático não bastar
  - **Preço mais fresco/ao vivo na abertura da página**: hoje o site é estático e mostra o fechamento oficial D-1 (série de fim de dia; tempo real é feed licenciado da B3, evitado de propósito). Atualizar o preço a cada acesso NÃO é possível no site estático — o navegador não pode chamar a B3 direto (CORS) e não há backend. O caminho certo é este item: um backend FastAPI que busca da B3 do lado do servidor (sem CORS, com cache) e decide deliberadamente o que fazer com a questão de tempo real/licenciamento

## Princípios que não mudam

- Todo número sai de código determinístico e testável; IA só interpreta texto.
- Toda afirmação tem fonte.
- Fatos e alertas — nunca recomendação de compra ou venda.
